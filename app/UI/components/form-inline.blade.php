<form id="{{$id}}" name="{{$id}}" {{$attributes}} class="ui form">
    <div class="ui card h-full w-full mb-2">
        <div class="flex-grow-1 content bg-white fields">
            {{$fields}}
            {{$buttons}}
        </div>
    </div>
</form>
